/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/6.7/userguide/multi_project_builds.html
 */
plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "0.5.0"
}

rootProject.name = 'steam-crossplatform-sync'
include('app')
include('game-session-tracker')
include('common')
include('steam-config-reader')

rootProject.children.each { project ->
    String fileBaseName = project.name
    project.projectDir = new File(settingsDir, "${fileBaseName}")
    project.buildFileName = "${fileBaseName}.gradle"
    assert project.projectDir.isDirectory()
    assert project.buildFile.isFile()
}
